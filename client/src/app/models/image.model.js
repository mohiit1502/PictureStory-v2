export default class Image {

    constructor(imageTitle, imageUrl, imageDescription, imageTags) {
        this.imageTitle = imageTitle;
        this.imageUrl = imageUrl;
        this.imageDescription = imageDescription;
        this.imageTags = imageTags
    }
}